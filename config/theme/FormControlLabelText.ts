import { createStyle } from "@gluestack-style/react";

export const FormControlLabelText = createStyle({
  fontWeight: "$medium",
  color: "$primary300",
  _dark: {
    color: "$textDark50",
  },
});
